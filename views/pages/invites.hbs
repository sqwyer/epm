<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>East Project Manager</title>

    <link rel="icon" type="image/x-icon" href="/static/favicon.ico">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
    {{> nav}}
    <div class="container short no-top">
        <div class="row gapped">
            {{> sidebar}}
            <div class="col fill top">
                <label class="page">@ Invites</label>
                <p class="light">This page is not yet finished...</p>
                <br />
                <hr />
                <br />
                <div class="invites">
                {{!-- {{#console user}}{{/console}} --}}
                {{!-- {{#each user.pending_projects}}
                {{#log}}{{this}}{{/log}}
                {{#project this}}
                    {{#log}}{{this}}{{/log}}
                    <div class="invite row gapped">
                        <p>{{project.name}}</p>
                        <button class="btn primary">
                            Join
                        </button>
                        <button class="btn danger" onclick="">
                            Ignore
                        </button>
                    </div>
                {{/project}}
                {{/each}} --}}
                {{#each invites}}
                    <div class="invite row gapped">
                        <p>{{name}}</p>
                        <button class="btn primary">
                            Join
                        </button>
                        <button class="btn danger" onclick="">
                            Ignore
                        </button>
                    </div>
                {{/each}}
                </div>
            </div>
        </div>
    </div>

    <script src="/js/index.js"></script>
    <script src="/js/post.js"></script>
    <script src="/js/invites.js"></script>
</body>
</html>